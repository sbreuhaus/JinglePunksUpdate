<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a href="http://jinglepunks.com/" class="navbar-brand" id="logo" href="#">Jingle Punks</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <%= form_tag(songs_path, :method => "get", :class => "navbar-form navbar-left") do %>
        <%= text_field_tag :search, params[:search], placeholder: "Enter a tag", class:"form-control" %>
        <%= submit_tag "Search", class: "btn btn-default" %>
        <%= link_to 'clear search', songs_path, class:"btn btn-default" %>
      <% end %>
      <ul class="nav navbar-nav navbar-left">
  
        <li><%= link_to 'Add a song!', new_song_path, id:"logo" %></li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
